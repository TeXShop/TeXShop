{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {
                putcustomized = id; 
                putenvironments = id; 
                putfunctions1 = id; 
                putgreek = id; 
                putintl = id; 
                putmath = id; 
                putsymbols = id; 
                puttypeface = id; 
            }; 
            CLASS = TSLaTeXPanelController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                custombuttonmatrix = id; 
                environbuttonmatrix = id; 
                functionsbuttonmatrix = id; 
                greekbuttonmatrix = id; 
                intlbuttonmatrix = id; 
                mathbuttonmatrix = id; 
                symbolsbuttonmatrix = id; 
                typefacebuttonmatrix = id; 
            }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}