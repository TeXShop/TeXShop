%%!TEX TS-program = pdflatexmk
%%!TEX encoding = UTF-8 Unicode
\documentclass{book}
\usepackage[utf8]{inputenc}

\usepackage{tmmlucida}

\usepackage{makeidx}
%\usepackage{index}
\makeindex

\title{A Simple Test of and Index}
 \author{by\\Herbert Schulz}
 \date{}

%\usepackage{hyperref}

\begin{document}
\chapter{The First Chapter}

\section{The first section of text}
Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}. Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an indéx\index{indéx}. Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index3} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}. XXXXXHere is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text1} and I'll make an index22\index{index22} at the end so it will create a simple test of creating an index\index{index}.YYYYYYHere is a bunch of text\index{text} and I'll make an index22\index{index22} at the end so it will create a simple test of creating an index\index{index3}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text2} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.

%\newpage
Here is a bunch of text\index{text} and I'll make an index\index{index} at the end so it will create a simple test of creating an index\index{index}.


\printindex
\end{document}
